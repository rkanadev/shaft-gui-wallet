{
  "name": "shaft-gui-wallet",
  "version": "0.0.1",
  "description": "Shaft GUI wallet",
  "main": "./main.js",
  "scripts": {
    "start": "electron .",
    "build": "cd renderer && npm run build",
    "build-binaries": "cd renderer && npm run build && cd .. && electron-builder --config build.json -l"
  },
  "repository": "https://code.shaft.sh/shaftsh/shaft",
  "keywords": [
    "shaft",
    "shaft-gui-wallet",
    "cryptocurrency"
  ],
  "author": "shaft.sh",
  "license": "CC0-1.0",
  "devDependencies": {
    "electron": "~1.7.9",
    "electron-builder": "^19.43.0"
  },
  "dependencies": {
    "bignumber.js": "^4.1.0",
    "electron-is-dev": "^0.3.0",
    "http_server": "^1.0.12",
    "make-dir": "^1.1.0",
    "request": "^2.81.1",
    "web3": "^0.18.4",
    "winston": "^3.0.0-rc1"
  }
}
